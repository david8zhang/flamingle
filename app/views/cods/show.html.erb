<%= render 'countries/index', cod: @cod, countries: @countries %>
<div class="cods center">
    <% @cods.each do |cod|%>
      <button class="cod center"> <%= link_to cod.name, cod_path %></button>
    <% end %>
</div>
